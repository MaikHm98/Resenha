# ── .NET ──────────────────────────────────────────────────────
bin/
obj/
*.user
*.suo
.vs/
*.db
*.sqlite

# Nunca subir appsettings com senhas reais
# appsettings.json local fica ignorado; arquivos versionados usam placeholders
backend/Resenha.API/appsettings.json

# Migrations geradas automaticamente (cada dev roda localmente)
backend/Resenha.API/Migrations/

# ── Node / React Native ────────────────────────────────────────
node_modules/
.expo/
dist/
build/
publish/
*.jks
*.keystore
*.p8
*.p12
*.mobileprovision
google-services.json
GoogleService-Info.plist

# ── Sistema ────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.log

# ── Claude Code (configs locais) ───────────────────────────────
.claude/

backend/Resenha.API/build_tmp/
tmp_build_api/
tmp_build_api_check/
frontend/resenha-app/.env
